function normalise(value){return String(value??'').trim().toLocaleLowerCase('en-GB').replace(/[’]/g,"'")}
function assertChallenges(challenges){if(!Array.isArray(challenges)||!challenges.length)throw new TypeError('Wordwall Tower needs challenges.')}
function questionsIn(challenge){return Array.isArray(challenge?.questions)?challenge.questions:[challenge]}
function flattenChallenges(challenges){return challenges.flatMap(questionsIn)}
function questionsPerLevel(challenges){return Math.max(1,questionsIn(challenges[0]).length)}

export const WORDWALL_SPIRAL=Object.freeze({radius:26,stageArc:Math.PI/3,stageRise:5,startAngle:-Math.PI/2});
export const WORDWALL_GATE_COLLIDER=Object.freeze({width:9,depth:1,height:3.5});
export const WORDWALL_TOKEN_STEPS=Object.freeze([2]);
export const WORDWALL_PLAYER_COLLIDER=Object.freeze({footRadius:.3,bodyRadius:.48,height:2.2});
export const WORDWALL_MIN_CLEARANCE=Object.freeze({box:1.1,circle:1.2,triangle:1.35,ring:1.05});
export const WORDWALL_MOVEMENT=Object.freeze({walkSpeed:8.4,jumpVelocity:9.6,gravity:19});
export const WORDWALL_SKINS=Object.freeze([
  Object.freeze({id:'starter',name:'Candy Climber',price:0,shirt:0xff6fcf,trousers:0x362e72,hair:0x35241f,accent:0xff6fcf}),
  Object.freeze({id:'ocean',name:'Ocean Explorer',price:5,shirt:0x37ddeb,trousers:0x1555a8,hair:0x12325b,accent:0x37ddeb}),
  Object.freeze({id:'solar',name:'Solar Sprinter',price:9,shirt:0xffc93f,trousers:0xe66032,hair:0x582814,accent:0xffc93f}),
  Object.freeze({id:'galaxy',name:'Galaxy Hero',price:14,shirt:0x895cff,trousers:0x252058,hair:0xff65c8,accent:0x895cff})
]);

export function getWordwallSpiralPose(stageIndex,progress=0,radialOffset=0,spiral=WORDWALL_SPIRAL){
  const routeProgress=stageIndex+Math.max(0,Math.min(1,progress)),angle=spiral.startAngle+routeProgress*spiral.stageArc,radius=spiral.radius+radialOffset,radialX=Math.cos(angle),radialZ=Math.sin(angle),forwardX=-radialZ,forwardZ=radialX;
  return{x:radialX*radius,z:radialZ*radius,top:routeProgress*spiral.stageRise,angle,yaw:-angle,radialX,radialZ,forwardX,forwardZ};
}

function worldToLocal(x,z,originX,originZ,yaw){const dx=x-originX,dz=z-originZ,c=Math.cos(yaw),s=Math.sin(yaw);return{x:c*dx-s*dz,z:s*dx+c*dz}}
function localToWorld(x,z,originX,originZ,yaw){const c=Math.cos(yaw),s=Math.sin(yaw);return{x:originX+c*x+s*z,z:originZ-s*x+c*z}}
function pointInTriangle(point,a,b,c){const sign=(p,p1,p2)=>(p.x-p2.x)*(p1.z-p2.z)-(p1.x-p2.x)*(p.z-p2.z),d1=sign(point,a,b),d2=sign(point,b,c),d3=sign(point,c,a),hasNegative=d1<0||d2<0||d3<0,hasPositive=d1>0||d2>0||d3>0;return!(hasNegative&&hasPositive)}
function distanceToSegment(point,a,b){const dx=b.x-a.x,dz=b.z-a.z,lengthSquared=dx*dx+dz*dz,t=lengthSquared?Math.max(0,Math.min(1,((point.x-a.x)*dx+(point.z-a.z)*dz)/lengthSquared)):0;return Math.hypot(point.x-(a.x+t*dx),point.z-(a.z+t*dz))}
function triangleVertices(width,depth){return[{x:0,z:-depth*2/3},{x:-width/2,z:depth/3},{x:width/2,z:depth/3}]}

export function getWordwallSupportClearance({shape='box',width,depth,holeRadius}){const safeWidth=Math.max(0,Number(width)||0),safeDepth=Math.max(0,Number(depth)||0);if(shape==='circle')return safeWidth/2;if(shape==='ring')return Math.max(0,safeWidth/2-(Number(holeRadius)||safeWidth*.17));if(shape==='triangle'){const vertices=triangleVertices(safeWidth,safeDepth),centre={x:0,z:0};return Math.min(...vertices.map((vertex,index)=>distanceToSegment(centre,vertex,vertices[(index+1)%3])))}return Math.min(safeWidth,safeDepth)/2}
export function sizeWordwallSupport({shape='box',width=0,depth=0,holeRadius},minimum=WORDWALL_MIN_CLEARANCE[shape]??WORDWALL_MIN_CLEARANCE.box){let sizedWidth=Math.max(.1,Number(width)||0),sizedDepth=Math.max(.1,Number(depth)||0);if(shape==='ring'){const diameter=Math.max(sizedWidth,sizedDepth,5.4),outerRadius=diameter/2,innerRadius=Math.max(.8,Math.min(Number(holeRadius)||outerRadius*.34,outerRadius-minimum));return{width:diameter,depth:diameter,holeRadius:innerRadius,clearance:outerRadius-innerRadius}}if(shape==='circle'){const diameter=Math.max(sizedWidth,sizedDepth,minimum*2);return{width:diameter,depth:diameter,clearance:diameter/2}}if(shape==='box'){sizedWidth=Math.max(sizedWidth,minimum*2);sizedDepth=Math.max(sizedDepth,minimum*2);return{width:sizedWidth,depth:sizedDepth,clearance:getWordwallSupportClearance({shape,width:sizedWidth,depth:sizedDepth})}}const clearance=getWordwallSupportClearance({shape,width:sizedWidth,depth:sizedDepth}),scale=clearance>0?Math.max(1,minimum/clearance):minimum*4;sizedWidth*=scale;sizedDepth*=scale;return{width:sizedWidth,depth:sizedDepth,clearance:getWordwallSupportClearance({shape,width:sizedWidth,depth:sizedDepth})}}

export function isWordwallFootprintOnSupport(x,z,support,radius=.3){
  if(!support?.solid)return false;const local=worldToLocal(x,z,support.x,support.z,support.yaw||0),distance=Math.hypot(local.x,local.z);if(support.shape==='circle')return distance<=support.width/2+radius+1e-9;if(support.shape==='ring'){const inner=Number(support.holeRadius)||support.width*.17;return distance<=support.width/2+radius+1e-9&&distance>=inner-radius-1e-9}if(support.shape==='triangle'){const vertices=triangleVertices(support.width,support.depth);return pointInTriangle(local,...vertices)||vertices.some((vertex,index)=>distanceToSegment(local,vertex,vertices[(index+1)%3])<=radius+1e-9)}const outsideX=Math.max(0,Math.abs(local.x)-support.width/2),outsideZ=Math.max(0,Math.abs(local.z)-support.depth/2);return Math.hypot(outsideX,outsideZ)<=radius+1e-9;
}

export function resolveWordwallSupportMovement({previousX,previousZ,nextX,nextZ,playerY},supports,{radius=WORDWALL_PLAYER_COLLIDER.bodyRadius,playerHeight=WORDWALL_PLAYER_COLLIDER.height,topEpsilon=.04}={}){let x=nextX,z=nextZ,blockedX=false,blockedZ=false;for(const support of supports||[]){if(!support?.solid)continue;const bottom=support.bottom??support.top-(support.height??.72);if(playerY>=support.top-topEpsilon||playerY+playerHeight<=bottom+topEpsilon)continue;const previousInside=isWordwallFootprintOnSupport(previousX,previousZ,support,radius);if(previousInside)continue;if(isWordwallFootprintOnSupport(x,previousZ,support,radius)){x=previousX;blockedX=true}if(isWordwallFootprintOnSupport(x,z,support,radius)){z=previousZ;blockedZ=true}if(isWordwallFootprintOnSupport(x,z,support,radius)){x=previousX;z=previousZ;blockedX=true;blockedZ=true}}return{x,z,blockedX,blockedZ}}
export function resolveWordwallSupportCeiling({previousY,nextY,x,z},supports,{radius=WORDWALL_PLAYER_COLLIDER.footRadius,playerHeight=WORDWALL_PLAYER_COLLIDER.height}={}){if(nextY<=previousY)return{y:nextY,blocked:false};const previousHead=previousY+playerHeight,nextHead=nextY+playerHeight;let ceiling=Infinity;for(const support of supports||[]){if(!support?.solid||!isWordwallFootprintOnSupport(x,z,support,radius))continue;const bottom=support.bottom??support.top-(support.height??.72);if(previousHead<=bottom+1e-9&&nextHead>=bottom-1e-9)ceiling=Math.min(ceiling,bottom)}return Number.isFinite(ceiling)?{y:ceiling-playerHeight,blocked:true}:{y:nextY,blocked:false}}

export function resolveWordwallGateMovement({previousX,previousZ,nextX,nextZ,playerY},gate,{radius=.3,playerHeight=2.2}={}){
  const clear={x:nextX,z:nextZ,blockedX:false,blockedZ:false};
  if(!gate||gate.open||playerY>=gate.bottom+gate.height||playerY+playerHeight<=gate.bottom)return clear;
  const yaw=gate.yaw||0,previous=worldToLocal(previousX,previousZ,gate.x,gate.z,yaw),next=worldToLocal(nextX,nextZ,gate.x,gate.z,yaw),halfWidth=gate.width/2+radius,halfDepth=gate.depth/2+radius,inside=(x,z)=>Math.abs(x)<=halfWidth&&Math.abs(z)<=halfDepth;
  let localX=next.x,localZ=previous.z;if(inside(localX,localZ))localX=previous.x;localZ=next.z;if(inside(localX,localZ))localZ=previous.z;if(inside(localX,localZ)){localX=previous.x;localZ=previous.z}const resolved=localToWorld(localX,localZ,gate.x,gate.z,yaw),blockedX=Math.abs(resolved.x-nextX)>1e-9,blockedZ=Math.abs(resolved.z-nextZ)>1e-9;return{x:resolved.x,z:resolved.z,blockedX,blockedZ};
}

export function createWordwallRun(challenges){assertChallenges(challenges);return{solvedCount:0,checkpointIndex:0,status:'running',activeChallenge:null,totalCorrect:0,attempts:1,completed:false}}
export function reachWordwallCheckpoint(state,challenges,checkpointIndex){assertChallenges(challenges);const questions=flattenChallenges(challenges);if(state.completed||state.status!=='running'||checkpointIndex!==state.solvedCount+1||checkpointIndex>questions.length)return state;return{...state,checkpointIndex,status:'challenge',activeChallenge:state.solvedCount,attempts:1}}
export function submitWordwallAnswer(state,challenges,answer){assertChallenges(challenges);if(state.status!=='challenge'||state.activeChallenge===null)return{state,correct:false,wallCompleted:false,levelCompleted:false};const questions=flattenChallenges(challenges),challenge=questions[state.activeChallenge],correct=normalise(answer)===normalise(challenge?.answer);if(!correct)return{state:{...state,attempts:state.attempts+1},correct:false,wallCompleted:false,levelCompleted:false};const solvedCount=state.solvedCount+1,totalCorrect=state.totalCorrect+1,perLevel=questionsPerLevel(challenges),levelCompleted=solvedCount%perLevel===0,completed=solvedCount===questions.length;return{state:{...state,solvedCount,status:completed?'complete':'running',activeChallenge:null,totalCorrect,attempts:1,completed},correct:true,wallCompleted:true,levelCompleted}}
export function respawnWordwallRun(state){return{...state,status:state.completed?'complete':'running',activeChallenge:null}}
export function getWordwallChallenge(state,challenges){assertChallenges(challenges);if(state.activeChallenge===null)return null;return flattenChallenges(challenges)[state.activeChallenge]||null}
export function createWordwallShareData(pageUrl,{worldName='Word Quest',levelCount=5,questionCount=25}={}){const url=new URL(pageUrl);url.searchParams.delete('preview');url.searchParams.delete('quality');url.hash='';return{title:'LexiClimb Tower achievement',text:`I cleared ${questionCount} learning walls across all ${levelCount} levels in ${worldName} on LexiClimb Tower!`,url:url.href}}

export function createWordwallProfile(saved={}){
  const source=saved&&typeof saved==='object'?saved:{},skinIds=new Set(WORDWALL_SKINS.map(skin=>skin.id)),owned=[...new Set(Array.isArray(source.ownedSkins)?source.ownedSkins.filter(id=>skinIds.has(id)):[])];if(!owned.includes('starter'))owned.unshift('starter');const equippedSkin=owned.includes(source.equippedSkin)?source.equippedSkin:'starter',collectedTokens=[...new Set(Array.isArray(source.collectedTokens)?source.collectedTokens.filter(id=>typeof id==='string'&&id.length<=40):[])];return{version:1,coins:Math.max(0,Math.floor(Number(source.coins)||0)),collectedTokens,ownedSkins:owned,equippedSkin};
}
export function createWordwallTokenId(stageIndex,stepIndex,scope=''){if(!Number.isInteger(stageIndex)||stageIndex<0||!Number.isInteger(stepIndex)||stepIndex<0)throw new RangeError('Token positions need non-negative integer indexes.');const prefix=String(scope).replace(/[^a-z0-9-]/gi,'').slice(0,24);return`${prefix?`${prefix}-`:''}stage-${stageIndex+1}-coin-${stepIndex+1}`}
export function collectWordwallToken(profile,tokenId,value=1){const current=createWordwallProfile(profile),id=String(tokenId);if(!id||current.collectedTokens.includes(id))return{profile:current,collected:false};return{profile:{...current,coins:current.coins+Math.max(1,Math.floor(Number(value)||1)),collectedTokens:[...current.collectedTokens,id]},collected:true}}
export function purchaseWordwallSkin(profile,skinId){const current=createWordwallProfile(profile),skin=WORDWALL_SKINS.find(item=>item.id===skinId);if(!skin)return{profile:current,purchased:false,reason:'missing'};if(current.ownedSkins.includes(skin.id))return{profile:{...current,equippedSkin:skin.id},purchased:false,reason:'owned'};if(current.coins<skin.price)return{profile:current,purchased:false,reason:'coins'};return{profile:{...current,coins:current.coins-skin.price,ownedSkins:[...current.ownedSkins,skin.id],equippedSkin:skin.id},purchased:true,reason:'purchased'}}
export function equipWordwallSkin(profile,skinId){const current=createWordwallProfile(profile);return current.ownedSkins.includes(skinId)?{...current,equippedSkin:skinId}:current}
